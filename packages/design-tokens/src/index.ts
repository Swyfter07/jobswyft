// Design tokens are generated at build time
// Import CSS variables in your app: @import '@jobswyft/design-tokens/tokens.css';

// Export tokens as a module (will be available after build)
export default {};
